[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "statarb-avellaneda-lee"
version = "1.0.0"
description = "PCA-based statistical arbitrage on Euro Stoxx 50, after Avellaneda & Lee (2008)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "matplotlib>=3.7",
]

[project.optional-dependencies]
public-data = [
    "yfinance>=0.2.40",
]
dev = [
    "pytest>=7.4",
    "ruff>=0.5",
    "jupyter>=1.0",
]

[tool.setuptools.packages.find]
include = ["statarb*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 90
target-version = "py310"
# Notebook cell sources are prose and plotting code; the line-length rule adds
# nothing there and build_notebook.py only holds those cells as string literals.
exclude = ["notebooks", "scripts/build_notebook.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
